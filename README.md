# Online Library Catalog 📚

Учебный проект микросервиса для управления каталогом библиотеки, разработанный на Go с использованием современных практик и технологий.

## 🚀 Технологии и подходы

### Backend
- **Go** - основной язык разработки
- **Fiber** - высокопроизводительный web framework
- **JWT** - аутентификация и авторизация
- **PostgreSQL** - реляционная база данных
- **Dependency Injection** - внедрение зависимостей
- **Clean Architecture** - многослойная архитектура

### Основные возможности
- ✅ RESTful API с CRUD операциями
- ✅ Аутентификация и авторизация (JWT)
- ✅ Ролевая модель (user/admin)
- ✅ Система оценок книг (0-5 звёзд)
- ✅ Сложные SQL запросы с JOIN и агрегацией
- ✅ Валидация данных
- ✅ Обработка ошибок

## 🏗 Архитектура

Проект демонстрирует принципы чистой архитектуры:

```
internal/
├── entity/      # Сущности предметной области
├── repository/  # Слой работы с данными (PostgreSQL)
├── service/     # Бизнес-логика
└── handler/     # HTTP обработчики (Fiber)
```

### Dependency Injection
Все зависимости передаются через конструкторы, что обеспечивает:
- Тестируемость кода
- Гибкость конфигурации
- Легкую замену компонентов

```go
// Пример DI в main.go
bookRepo := postgres.NewBookRepository(db)
ratingService := service.NewRatingService(ratingRepo, bookRepo)
bookService := service.NewBookService(bookRepo, ratingService)
bookHandler := v1.NewBookHandler(bookService)
```

## 📊 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - регистрация
- `POST /api/v1/auth/login` - вход
- `GET /api/v1/auth/profile` - профиль пользователя

### Книги (публичные)
- `GET /api/v1/books` - список книг с рейтингами
- `GET /api/v1/books/:id` - информация о книге

### Книги (защищенные)
- `POST /api/v1/user/books` - создать книгу
- `PUT /api/v1/user/books/:id` - обновить книгу
- `DELETE /api/v1/user/books/:id` - удалить книгу

### Авторы
- `GET /api/v1/authors` - список авторов
- `GET /api/v1/authors/:id` - информация об авторе
- `GET /api/v1/authors/:id/books` - книги автора

### Оценки (требуют аутентификации)
- `POST /api/v1/ratings` - поставить оценку книге
- `GET /api/v1/ratings/my` - мои оценки
- `GET /api/v1/ratings/book/:book_id` - моя оценка книги
- `DELETE /api/v1/ratings/book/:book_id` - удалить оценку

## 🗄 База данных

### Схема данных
```sql
-- Пользователи
users (id, email, password_hash, role, created_at, updated_at)

-- Авторы
authors (id, first_name, last_name, biography, birth_date, created_at, updated_at)

-- Книги
books (id, title, author_id, isbn, description, published_at, created_at, updated_at)

-- Оценки
ratings (id, user_id, book_id, score, created_at, updated_at)
```

### Ключевые SQL особенности
- **JOIN операции** - получение книг с информацией об авторах
- **Агрегатные функции** - расчет среднего рейтинга
- **UNIQUE constraints** - один пользователь = одна оценка на книгу
- **Foreign keys** - целостность данных
- **Индексы** - оптимизация запросов

## 🛠 Установка и запуск

### Требования
- Go 1.21+
- PostgreSQL 12+
- Git

### Шаги установки
1. Клонировать репозиторий:
```bash
git clone https://github.com/yourusername/online-library-catalog.git
cd online-library-catalog
```

2. Настроить базу данных:
```sql
CREATE DATABASE library_db;
CREATE USER library_user WITH PASSWORD 'library_password';
GRANT ALL PRIVILEGES ON DATABASE library_db TO library_user;
```

3. Создать файл окружения:
```env
APP_PORT=:8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=library_user
DB_PASSWORD=library_password
DB_NAME=library_db
DB_SSLMODE=disable
JWT_SECRET=your-super-secret-jwt-key
```

4. Запустить приложение:
```bash
go mod tidy
go run cmd/api/main.go
```

## 🧪 Тестирование

Используйте Postman коллекцию для тестирования API:

1. Импортируйте `postman_collection.json`
2. Настройте environment variables
3. Запустите тесты в правильной последовательности

## 📚 Чему посвящен проект

### Go практики
- Работа с горутинами и каналами
- Использование context для управления временем жизни
- Обработка ошибок в Go стиле
- Работа с пакетами и модулями

### Backend разработка
- Проектирование REST API
- Аутентификация и авторизация
- Работа с реляционными базами данных
- Валидация и обработка входных данных

### Паттерны проектирования
- **Repository pattern** - абстракция доступа к данным
- **Dependency Injection** - управление зависимостями
- **Middleware** - перехватчики запросов
- **Clean Architecture** - разделение ответственности

## 🎯 Цели обучения

Этот проект демонстрирует:
- Понимание конкурентности в Go (goroutines, channels)
- Умение работать с SQL и реляционными базами
- Навыки проектирования RESTful API
- Применение принципов чистой архитектуры
- Практику внедрения зависимостей
- Работу с JWT и middleware

## 📞 Контакты

Проект разработан в учебных целях для демонстрации навыков backend-разработки на Go.

---
*Этот проект является частью портфолио и демонстрирует практические навыки разработки микросервисов на Go.*